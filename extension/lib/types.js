/**
 * Shared Types for Chrome Extension
 * Copied from src/types/word.types.ts
 */

const WordTypes = {
  // Type definitions as constants for runtime use
}

// Export for use in other scripts
if (typeof module !== 'undefined' && module.exports) {
  module.exports = WordTypes
}
