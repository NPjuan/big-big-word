# 🚀 Chrome 插件快速安装指南

## 📋 安装前准备

### 1. 准备图标文件

由于 Chrome 扩展需要图标文件，请先准备图标：

**临时方案**（用于测试）：
可以从网上下载任意 PNG 图标，重命名为以下文件名并放入 `extension/icons/` 目录：

- `icon16.png`
- `icon32.png`
- `icon48.png`
- `icon128.png`

**推荐图标**：

- 使用 📚 书本 emoji 截图
- 或者使用纯色背景 + "BBW" 文字

---

## 🔧 安装步骤

### Step 1: 打开 Chrome 扩展管理页面

在 Chrome 浏览器地址栏输入：

```
chrome://extensions/
```

或者通过菜单：

```
Chrome 菜单 → 更多工具 → 扩展程序
```

### Step 2: 启用开发者模式

在扩展管理页面右上角，找到并**打开**"开发者模式"开关。

### Step 3: 加载扩展

1. 点击左上角的"**加载已解压的扩展程序**"按钮
2. 在文件选择器中，导航到项目目录
3. 选择 `big-big-word/extension` 文件夹
4. 点击"**选择文件夹**"

### Step 4: 确认安装

安装成功后，你会看到：

- ✅ 扩展列表中出现 "Big Big Word - 单词学习助手"
- ✅ 浏览器工具栏出现插件图标
- ✅ 扩展状态显示为"已启用"

### Step 5: 固定到工具栏（可选）

1. 点击浏览器工具栏右侧的拼图图标（扩展图标）
2. 找到 "Big Big Word"
3. 点击图钉图标，将其固定到工具栏

---

## ✅ 测试插件

### 测试 1: 选词翻译

1. 打开任意英文网页（如 Wikipedia）
2. 用鼠标选中一个英文单词（如 "serendipity"）
3. 等待 300ms，应该会出现翻译弹窗
4. 查看音标、释义等信息

### 测试 2: 添加单词

1. 在翻译弹窗中点击"📚 添加到单词本"
2. 应该显示"✅ 已添加到单词本"提示
3. 弹窗自动关闭

### 测试 3: 查看单词列表

1. 点击浏览器工具栏的插件图标
2. 应该看到刚才添加的单词
3. 单词数量显示为 1

### 测试 4: 手动添加单词

1. 在弹窗的输入框中输入 "ephemeral"
2. 点击"添加"按钮或按 Enter
3. 等待加载，单词应该出现在列表中

### 测试 5: 右键菜单

1. 在网页上选中一个单词
2. 右键点击
3. 应该看到"翻译 [单词]"选项
4. 点击后显示翻译弹窗

### 测试 6: 快捷键

1. 在网页上选中一个单词
2. 按 `Ctrl+Shift+T` (Windows) 或 `Cmd+Shift+T` (Mac)
3. 应该显示翻译弹窗

---

## 🐛 故障排除

### 问题 1: 无法加载扩展

**错误信息**: "无法加载扩展"

**解决方案**:

1. 检查是否选择了正确的 `extension` 文件夹
2. 确保 `manifest.json` 文件存在
3. 检查 manifest.json 语法是否正确

### 问题 2: 图标显示错误

**错误信息**: "无法加载图标"

**解决方案**:

1. 确保 `extension/icons/` 目录下有所有图标文件
2. 图标文件名必须完全匹配：`icon16.png`, `icon32.png`, `icon48.png`, `icon128.png`
3. 临时方案：可以注释掉 manifest.json 中的 icons 配置

### 问题 3: 选词后没有弹窗

**可能原因**:

1. Content Script 未正确注入
2. 网页有 CSP 限制

**解决方案**:

1. 刷新网页（F5）
2. 打开浏览器控制台（F12）查看错误信息
3. 在扩展管理页面点击"重新加载"按钮

### 问题 4: API 请求失败

**错误信息**: "Dictionary API error"

**解决方案**:

1. 检查网络连接
2. 确认可以访问 `https://api.dictionaryapi.dev/`
3. 查看浏览器控制台的详细错误信息

### 问题 5: 数据未保存

**可能原因**: Chrome Storage 权限问题

**解决方案**:

1. 检查 manifest.json 中是否有 "storage" 权限
2. 重新加载扩展
3. 清除浏览器缓存后重试

---

## 🔄 更新扩展

修改代码后，需要重新加载扩展：

### 方法 1: 扩展管理页面

1. 打开 `chrome://extensions/`
2. 找到 "Big Big Word"
3. 点击"重新加载"按钮（圆形箭头图标）

### 方法 2: 快捷键

1. 在扩展管理页面
2. 按 `Ctrl+R` (Windows) 或 `Cmd+R` (Mac)

### 注意事项

- 修改 Content Script 后，需要刷新网页才能生效
- 修改 Popup 页面后，需要关闭并重新打开弹窗
- 修改 Background Script 后，需要重新加载扩展

---

## 📝 调试技巧

### 调试 Content Script

1. 在网页上右键 → "检查"
2. 打开 Console 标签
3. 查看 "Big Big Word extension loaded" 消息
4. 查看错误信息

### 调试 Popup

1. 右键点击插件图标
2. 选择"检查弹出内容"
3. 打开 DevTools 查看 Console 和 Network

### 调试 Background Script

1. 打开 `chrome://extensions/`
2. 找到 "Big Big Word"
3. 点击"服务工作进程"下的"检查视图"
4. 查看 Console 输出

### 查看存储数据

在 Console 中执行：

```javascript
chrome.storage.local.get(['big-big-words'], (result) => {
  console.log(result)
})
```

---

## 🎯 下一步

安装成功后，你可以：

1. **测试所有功能**
   - 选词翻译
   - 添加单词
   - 管理单词本
   - 快捷键和右键菜单

2. **自定义配置**
   - 修改快捷键
   - 调整样式
   - 更改 Web 应用 URL

3. **准备发布**
   - 设计专业图标
   - 完善功能
   - 准备截图和说明

4. **发布到 Chrome Web Store**
   - 注册开发者账号
   - 打包扩展
   - 提交审核

---

## 📚 相关文档

- [Chrome Extension 官方文档](https://developer.chrome.com/docs/extensions/)
- [Manifest V3 迁移指南](https://developer.chrome.com/docs/extensions/mv3/intro/)
- [Chrome Storage API](https://developer.chrome.com/docs/extensions/reference/storage/)
- [Content Scripts](https://developer.chrome.com/docs/extensions/mv3/content_scripts/)

---

**祝你使用愉快！如有问题，请查看 README.md 或提交 Issue。** 🎉
